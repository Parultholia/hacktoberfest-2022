import { SxProps } from "@mui/material";

const styles: {
  [key: string]: SxProps;
} = {
  btn: {
    width: "310px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    borderRadius: "20px",
    backgroundColor: "#8A2BE2",
  },
};

export default styles;
