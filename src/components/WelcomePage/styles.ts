import { SxProps } from "@mui/material";

const styles: {
  [key: string]: SxProps;
} = {
  content: {
    fontSize: "56px",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    height: "70vh",
  },

  startBtn : {
    display: "block",
  },
};

export default styles;
