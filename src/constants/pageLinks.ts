type typePageLinks = {
  [key: string]: string;
};

const pageLinks: typePageLinks = {
  basicInfoSrc: "/register/basic-info",
  professionalInfoSrc: "/register/professional-info",
};

export default pageLinks;
